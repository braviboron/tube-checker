# Normalise line endings: store LF in the repo, check out native on Windows.
* text=auto eol=lf
*.html text eol=lf
*.md   text eol=lf
*.sh   text eol=lf

# Vendored OCR assets — treat as binary (no EOL normalisation / diffing).
*.gz                   binary
vendor/tesseract/*.js  -text
